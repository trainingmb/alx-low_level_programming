#include "main.h"

/**
 * _isdigit - Checks if the passed inttger is a number
 * @c: The letter to test
 * Return: Returns 1 if is number else return 0
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
