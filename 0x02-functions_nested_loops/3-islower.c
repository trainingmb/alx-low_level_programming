#include "main.h"

/**
 * _islower; Checks if the passed inttger is a lower case letter
 * @c: The letter to test
 *
 * Return: Returns 1 if is lower case else return 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
