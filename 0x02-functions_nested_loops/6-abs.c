#include "main.h"

/**
 * _abs - Provides the absolute value of the integer passed in
 * @n: The number to be tested
 * Return:	Returns the absolute value of n
 */
int _abs(int n)
{
	return (n < 0 ? -1 * n : n); 
}
